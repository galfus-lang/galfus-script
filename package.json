{
  "name": "scripts",
  "type": "module",
  "private": true,
  "scripts": {
    "cmd": "bun scripts/cmd.ts",
    "check": "cargo check --all --all-targets && cargo clippy --all --all-targets -- -D warnings",
    "format": "prettier --write . && cargo fmt --all"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  }
}
